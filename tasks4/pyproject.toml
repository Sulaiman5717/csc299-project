[project]
name = "tasks4"
version = "0.1.0"
description = "OpenAI Chat Completions API experiment for task summarization"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=1.0.0",
]

[project.scripts]
tasks4 = "main:main"
